import React from 'react';
import AdvertiseInfo from '../advertiseinfo/AdvertiseInfo';

const Advertise = ({adver}) => {
    console.log(adver)
    return (
        <div>
        
        {/* {adver.advertise=== "yes" &&
            adver.map(at=><AdvertiseInfo at={at}></AdvertiseInfo>) */}
        {/* } */}
        </div>
    );
};

export default Advertise;