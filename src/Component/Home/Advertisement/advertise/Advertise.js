import React from 'react';

const Advertise = ({adver}) => {
    return (
        <div>
          {/* {adver.map(at=> at.advertise==="yes"&&
          <p>{at.name}</p>
          )} */}
        </div>
    );
};

export default Advertise;